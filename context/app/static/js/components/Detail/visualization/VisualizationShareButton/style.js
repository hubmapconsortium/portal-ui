import styled from 'styled-components';
import { WhiteBackgroundIconButton } from 'js/shared-styles/buttons';

const StyledWhiteButton = styled(WhiteBackgroundIconButton)`
  margin-left: 0.5rem;
`;

export { StyledWhiteButton };
