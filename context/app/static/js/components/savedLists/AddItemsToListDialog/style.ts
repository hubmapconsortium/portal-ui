import { styled } from '@mui/material/styles';
import { ListsIcon } from 'js/shared-styles/icons';

const StyledListsIcon = styled(ListsIcon)(({ theme }) => ({
  marginRight: theme.spacing(0.5),
}));

export { StyledListsIcon };
