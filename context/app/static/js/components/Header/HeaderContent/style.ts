import { styled } from '@mui/material/styles';
import Button from '@mui/material/Button';
import Box from '@mui/material/Box';

const Spacer = styled(Box)({
  flexGrow: 1,
});

const HeaderButton = styled(Button)({
  color: 'white',
});

export { Spacer, HeaderButton };
