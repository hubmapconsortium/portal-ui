import Typography from '@mui/material/Typography';
import { styled } from '@mui/material/styles';

const StyledTypography = styled(Typography)({
  fontSize: 'clamp(10px, 2.5vw, 0.875rem)',
  lineHeight: 1.2,
});

export { StyledTypography };
