import styled from 'styled-components';
import IconButton from '@material-ui/core/IconButton';

const BackgroundIconButton = styled(IconButton)`
  background-color: #fff;
  padding: 10px;
  border-radius: 4px;
`;

export { BackgroundIconButton };
