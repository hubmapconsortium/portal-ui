import { DatasetIcon, SampleIcon, DonorIcon } from 'js/shared-styles/icons';

export const entityIconMap = {
  Donor: DonorIcon,
  Sample: SampleIcon,
  Dataset: DatasetIcon,
  Support: DatasetIcon,
};
