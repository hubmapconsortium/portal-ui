import { styled } from '@mui/material/styles';

const CenteredLoaderWrapper = styled('div')({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});

export { CenteredLoaderWrapper };
