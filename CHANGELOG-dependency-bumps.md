- Bumped dependencies to resolve Dependabot alerts: `uuid` (^14), `pytest` (>=9.0.3), `lxml` (>=6.1.0); pnpm overrides for `piscina`, `lodash`, `minimatch`; uv constraints for `aiohttp`, `tornado`, `cryptography`, `pyjwt`, `idna`, `urllib3`, `pillow`. Two alerts are deferred: `ajv` (the only fix is ajv@8, which breaks `@eslint/eslintrc`, still pinned to ajv@6 — dev/lint-time only) and `vega-functions` (pinned by `vitessce`'s bundled `react-vega`/`vega@5` stack, needs a vitessce upgrade).
