- Bumped dependencies to resolve Dependabot alerts: `vite` (^8.0.16), `vitest`/`@vitest/ui` (^4.1.11), `svgo`, `fflate`, `postcss-selector-parser` and `immutable` via the lockfile; uv constraints raised for `aiohttp`, `tornado`, `cryptography` and `pillow`. Also moved the `onlyBuiltDependencies` and `overrides` settings from `package.json` to `pnpm-workspace.yaml`, since pnpm 10.27 no longer reads them from `package.json`.
