language: python
python:
  - '3.7'
cache: pip
before_install:
  - nvm install 12.16.3
env:
  COMMONS_BRANCH=devel
install:
  - printenv
  - pip install -r context/requirements.txt
  - echo 'POINT 1'
script:
  - git config --global submodule.recurse true # Make dev-start.sh happy.
  - ./test.sh
